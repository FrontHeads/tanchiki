export type GameSettings = {
  width: number;
  height: number;
  boundarySize: number;
  indicatorsSidebarSize: number;
};

export type StatisticsData = {
  score: number;
  matches: number;
  time: number;
};
