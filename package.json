{
  "name": "tanchiki",
  "private": true,
  "scripts": {
    "bootstrap": "yarn && node init.d/app-init.js && lerna clean && yarn && lerna bootstrap && git config --local commit.template .gitcommittemplate",
    "build:client": "lerna run build --scope=client",
    "build:server": "lerna run build --scope=server",
    "build": "yarn build:client && yarn build:server",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "preview": "yarn build && lerna run preview --parallel",
    "docker": "docker-compose up --build -d",
    "docker:build": "docker-compose build",
    "docker:pgadmin": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up pgadmin -d",
    "docker:mongo-express": "docker-compose -f docker-compose.yml -f docker-compose.dev.yml up mongo-express -d",
    "docker:dev": "yarn docker:pgadmin && yarn docker:mongo-express && yarn dev"
  },
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=19"
  },
  "devDependencies": {
    "@commitlint/cli": "17.3.0",
    "@commitlint/config-conventional": "17.3.0",
    "@evilmartians/lefthook": "1.1.1",
    "lerna": "5.4.3"
  }
}
